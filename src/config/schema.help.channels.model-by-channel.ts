export const CHANNELS_MODEL_BY_CHANNEL_FIELD_HELP: Record<string, string> = {
  "channels.modelByChannel":
    "Map provider -> channel id -> model override (values are provider/model or aliases).",
};
